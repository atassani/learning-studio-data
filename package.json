{
  "name": "learning-studio-data",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev:data": "serve --cors --listen 4173 data",
    "dev:data:test": "serve --cors --listen 4174 test-data",
    "audit:lang": "node scripts/audit-language-data.mjs",
    "audit:lang:strict": "node scripts/audit-language-data.mjs --strict-translation"
  },
  "devDependencies": {
    "serve": "^14.2.5"
  }
}
